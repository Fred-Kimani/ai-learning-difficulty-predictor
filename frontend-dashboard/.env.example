# === Backend API Connection ===
# URL of the FastAPI backend (no trailing slash).
VITE_API_URL=http://127.0.0.1:8000

# === API Key ===
# If the backend requires an API key, set it here.
# Leave blank if auth is disabled.
VITE_API_KEY=
